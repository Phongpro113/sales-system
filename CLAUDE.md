- project use docker compose (docker-compose.yml) (golang:1.26.2-alpine), microservices, frontend (react), backend (golang), database (postgresql), and message broker (kafka). The project is organized into separate directories for each service
- admin-service use Gin framework v1.9.1
frontend in `frontend/` use tanstack/react-table, primereact, tailwindcss
- read files in folder .github/rules
- Code in an extensible way